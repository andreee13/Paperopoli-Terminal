enum ShipStatus {
  docked,
  arriving,
  leaving,
}

enum GoodStatus {
  onShip,
  onGround,
  leaved,
}

enum VehicleStatus {
  inside,
  outside,
}

enum PersonStatus {
  onShip,
  onGround,
  onVehicle,
}
