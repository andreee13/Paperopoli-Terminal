enum ShipStatus {
  docked,
  arriving,
  leaving,
  none,
}

enum GoodStatus {
  onShip,
  onGround,
  leaved,
  none,
}

enum VehicleStatus {
  inside,
  outside,
  none,
}

enum PersonStatus {
  onShip,
  onGround,
  onVehicle,
  none,
}

enum OperationStatus {
  working,
  waiting,
  done,
  none,
}
